import Link from "next/link";import {DesignedPlaceholder,Intro,Page} from "@/components/Site";import {getPosts} from "@/lib/posts";export const metadata={title:"Writing"};export default function Blog(){const posts=getPosts();return <Page><Intro kicker="WRITING" title="Ideas you can use in your digital business." copy="Read practical notes on websites, automation and technology choices through the lens of the outcome you need."/><section className="blog-list">{posts.length?posts.map(p=><article key={p.slug}><p className="tag">{p.date}</p><div><h2><Link href={`/blog/${p.slug}`}>{p.title}</Link></h2><p>{p.excerpt}</p></div></article>):<DesignedPlaceholder label="Blog source migration"/>}</section></Page>}
