import Link from "next/link";import {Intro,Page} from "@/components/Site";import {getPosts} from "@/lib/posts";export const metadata={title:"Writing"};export default function Blog(){const posts=getPosts();return <Page><Intro kicker="WRITING" title="Notes on building useful digital work." copy="Existing articles from Karan’s previous portfolio will live here at their original slugs once the source files are available."/><section className="blog-list">{posts.length?posts.map(p=><article key={p.slug}><p className="tag">{p.date}</p><div><h2><Link href={`/blog/${p.slug}`}>{p.title}</Link></h2><p>{p.excerpt}</p></div></article>):<div className="placeholder"><span>AWAITING SOURCE ACCESS</span><p>Blog migration is ready, but GitHub access was blocked in the build environment. No post titles or slugs have been invented.</p></div>}</section></Page>}
